<div class="container-fluid">
    <div class="row header">
    <%# ECサイトタイトル %>
        <div class="col-xs-12">
            <%= link_to root_path, class: "header-link" do %><h1>MACALLAN</h1><% end %>
        </div>
    <%# ハンバーガーメニュー機能 %>
        <div id="humburger-icon">
        <a href="#" class="menu-trigger">
            <span></span>
            <span></span>
            <span></span>
        </a>
        </div>
    <%# ログイン機能 %>
        <div class="login-status">
            <% if end_user_signed_in? %>
            <% else %>
            <%= link_to "ログイン", new_end_user_session_path, class: "header-link" %>
            <%= link_to "新規登録", new_end_user_registration_path, class: "header-link" %>
            <% end %>
        </div>
    <%# 検索フォーム機能 %>
            <%= search_form_for(@search) do |s| %>
            <%= s.search_field :album_or_discs_songs_artist_name_or_label_name_or_category_name_cont %>
            <%= s.submit %>
            <% end %>

    </div>
</div>
