<h2>Topページ</h2>
<div class="container clearfix">
    <div class="row production">
    <!-- ここからジャケット写真 -->
    <%= @items.each do |item| %>
        <div class="col-xs-4 image1">
            <%= attachment_image_tag item, :image, :fill, 100, 100, fallback: "no_image.jpg", size:"100x100", class: "images"%>
            <p><%= item.album %></p>

            <p>¥<%= item.price %>(税込)</p>
        </div>
        <div class="col-xs-4  image2">
            <%= attachment_image_tag item, :image, :fill, 100, 100, fallback: "no_image.jpg", size:"100x100"%>
            <p><%= item.album %></p>

            <p>¥<%= item.price %>(税込)</p>
        </div>
        <div class="col-xs-4 image3">
            <%= attachment_image_tag item, :image, :fill, 100, 100, fallback: "no_image.jpg", size:"100x100"%>
            <p><%= item.album %></p>

            <p>¥<%= item.price %>(税込)</p>
        </div>
        <div class="col-xs-4 image4">
            <%= attachment_image_tag item, :image, :fill, 100, 100, fallback: "no_image.jpg", size:"100x100"%>
            <p><%= item.album %></p>

            <p>¥<%= item.price %>(税込)</p>
        </div>
        <div class="col-xs-4 image5">
            <%= attachment_image_tag item, :image, :fill, 100, 100, fallback: "no_image.jpg", size:"100x100"%>
            <p><%= item.album %></p>

            <p>¥<%= item.price %>(税込)</p>
        </div>
        <div class="col-xs-4 image6">
            <%= attachment_image_tag item, :image, :fill, 100, 100, fallback: "no_image.jpg", size:"100x100"%>
            <p><%= item.album %></p>

            <p>¥<%= item.price %>(税込)</p>
        </div>
    <% end %>
    <!-- ここまでジャケット写真 -->
    </div>
</div>

<!-- ページネイション -->

<div class="page">
<div class="pagination">
    <p><<</p>
    <p><</p>
    <p>1</p>
    <p>2</p>
    <p>3</p>
    <p>></p>
    <p>>></p>
</div>
</div>
